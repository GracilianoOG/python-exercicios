# Turples are immutable, they can't be changed
mytuple = ("x", "y")
print(mytuple)

# Parentheses are optional
mytuple = "x", "y"
print(mytuple)

# One element needs a comma to work properly
mytuple = ("x",)
print(mytuple)